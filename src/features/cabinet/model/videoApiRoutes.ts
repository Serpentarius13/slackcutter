export const videoApiRoutes = {
  video: "/api/video",
  videos: "/api/videos",
  clip: "/clip",
  clips: "/clips",
  downloadVideo: `/api/video/download`,
  downloadClip: `/api/clip/download`,
} as const;
