export type TNavLink = {
  link: string;
  text: string;
};

export interface IHeroCard {
  img: string;
  alt: string;
  text: string;
}

export interface IImage {
  imgSource: string;
  imgAlt: string;
}
